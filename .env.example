# Copy this file to .env in the repo root and fill in real values before running Prisma.
# Do NOT commit secrets. Keep real credentials out of the repo.

# Postgres example (replace placeholders):
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"
# DIRECT_URL is optional and used for certain deployments; copy if you need it.
# DIRECT_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"

# Example (uncomment and replace values):
# DATABASE_URL="postgresql://dbuser:password@localhost:5432/gocart?schema=public"
# DIRECT_URL="postgresql://dbuser:password@localhost:5432/gocart"

# For quick local testing you may use SQLite instead (adjust prisma/schema.prisma accordingly):
# DATABASE_URL="file:./dev.db"

# Public runtime values (examples):
# Use simple KEY=VALUE format. Do NOT commit real secrets.
NEXT_PUBLIC_CURRENCY_SYMBOL='$'
